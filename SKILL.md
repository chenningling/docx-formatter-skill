---
name: docx-formatter
description: >
  对已有 Word 文档（.docx）进行中文排版，支持两种标准格式：
  （1）中文学术论文——适用于毕业论文、学位论文、研究报告；
  （2）中文公文——适用于行政公文，遵循 GB/T 9704-2012 标准。
  
  当用户上传 .docx 文件并提出以下任何需求时，请立即使用本技能：
  排版、套格式、按规范格式化、改成学术论文样式、改成公文格式、
  "帮我把这个文档按论文格式排一下"、"这份文件需要用公文格式"、
  "reformat to Chinese academic style"、"apply thesis formatting"。
  只要用户有一个待处理的 Word 文档并提到格式/排版/样式，即使没有明说
  "docx"也应触发本技能。
---

# docx-formatter

将用户的 Word 文档格式化为标准中文学术论文或公文样式，返回排版后的 .docx 文件。

## 环境准备（首次使用）

```bash
bash <skill_dir>/scripts/setup.sh
```

## 工作流程

### 第 1 步：确认目标样式

从用户请求中判断：

| 用户说的关键词 | 目标样式 | 参数值 |
|---|---|---|
| 学术论文、毕业论文、论文、thesis、paper、academic | 学术论文 | `--style academic` |
| 公文、行政文件、政府文件、官方文件、gongwen | 公文 | `--style gongwen` |

如果无法判断，询问用户：
> "请问需要排版为哪种格式？（1）中文学术论文  （2）中文公文"

### 第 2 步：确认输入文件

确认用户提供了 .docx 文件路径。如果是 .doc 格式，先提示用户另存为 .docx。

### 第 3 步：提取段落列表并进行语义分析

运行提取脚本，读取所有段落：

```bash
python <skill_dir>/scripts/extract_paragraphs.py --input <用户文件路径.docx>
```

仔细阅读输出的段落列表，**用你自己的语义理解**判断每个段落的层级类型，生成 `heading_map`：

- **title**：文档总标题（通常是开头最短、最重要的那一行，如论文题目、公文标题）
- **h1**：章级标题（如"第一章 绪论"、"一、总体要求"、"Abstract"、"前言"、"结论"）
- **h2**：节级标题（如"1.1 研究背景"、"（一）主要措施"）
- **h3**：小节标题（如"1.1.1 深度学习的优势"、"1. 制度要求"）
- **body**：正文（默认，不需填入 map）

判断依据除了编号模式，还要考虑语义：
- 短段落（≤30字）且独立成行，极有可能是标题
- "前言"、"摘要"、"Abstract"、"结论"、"致谢"、"参考文献"等即使无编号也是 h1
- 正文里偶尔出现的带编号短句（如条款列表"1. 申请人需提供..."）不是标题

生成的 heading_map 示例：
```json
{"0": "title", "4": "h1", "11": "h1", "14": "h2", "18": "h3"}
```

### 第 4 步：携带语义映射运行格式化脚本

```bash
python <skill_dir>/scripts/format_docx.py \
  --input       <用户文件路径.docx> \
  --style       academic \
  --heading-map '<上一步生成的 JSON>' \
  --output      <输出文件路径.docx>
```

输出文件命名规则：在原文件名末尾加 `_formatted`，例如 `paper_formatted.docx`。

### 第 5 步：向用户汇报结果

返回格式化后的文件，并简要说明：
- 应用了哪种样式
- 识别到的文档结构（有几个章节标题、几个节标题）
- 有无需要用户确认的判断（例如"我将'前言'识别为一级标题，如有误请告知"）

## 格式化范围（方案 A）

**会处理的内容：**
- 正文段落：字体、字号、行距、首行缩进、段落间距
- 标题段落（H1/H2/H3）：字体、字号、加粗、对齐方式、段前段后间距
- 文档标题（Title 样式）：特殊格式（论文封面标题 / 公文发文标题）
- 页面边距（所有分节）

**不会处理的内容（保持原样）：**
- 表格及其内容
- 图片和图形
- 页眉页脚
- 脚注尾注
- 批注和修订记录

这样做的原因：表格和图片的原始格式通常由用户精心设计，强制套用样式容易破坏布局。

## 样式参数速查

需要向用户解释具体格式规范时，加载对应参考文件：
- 学术论文：`references/academic_style.md`
- 公文：`references/gongwen_style.md`

## 常见问题

| 问题 | 解决方法 |
|---|---|
| `No module named 'docx'` | 运行 `setup.sh` 安装依赖 |
| 文档没有标题 | 正常现象，所有段落按正文处理 |
| 公文特殊字体（方正小标宋）不显示 | 字体名称已正确写入文件，在安装了该字体的机器上打开即可正常显示 |
| 输入是 .doc 而非 .docx | 在 Word/WPS 中另存为 .docx 格式后再处理 |
